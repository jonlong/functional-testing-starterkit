/**
 * Secrets
 *
 * (Rename `secrets.sample.js` to `secrets.js` to use)
 *
 * An alternative location for secret creds, in case storing them in environment
 * variables isn't doing it for you.
 */

module.exports = {
  browserstack: {
    username: 'sample-username',
    accessKey: 'sample-accesskey'
  }
};